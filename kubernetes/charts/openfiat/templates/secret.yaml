apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
type: Opaque
data: {}
# Populate via `helm upgrade --set-file` or an external secrets operator —
# no secrets are committed to this chart.
