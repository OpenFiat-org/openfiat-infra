# monitoring

Observability stack for OpenFiat infrastructure: Prometheus (metrics),
Grafana (dashboards), Loki (logs), Tempo (traces), Alertmanager (alerting),
and an OpenTelemetry Collector as the common ingestion point.

```bash
docker compose up
```
